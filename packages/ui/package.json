{
  "name": "@repo/ui",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./*": "./src/*.tsx"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "generate:component": "turbo gen react-component",
    "check-types": "tsc --noEmit",
    "test": "echo 'No tests configured for @repo/ui package'",
    "test:watch": "echo 'No tests configured for @repo/ui package'",
    "test:coverage": "echo 'No tests configured for @repo/ui package'",
    "test:coverage:watch": "echo 'No tests configured for @repo/ui package'",
    "test:ci": "echo 'No tests configured for @repo/ui package'",
    "security:audit": "echo 'No security audit needed for @repo/ui package'",
    "security:check": "echo 'No security check needed for @repo/ui package'",
    "security:test": "echo 'No security tests needed for @repo/ui package'",
    "accessibility:test": "echo 'UI components should be tested in consuming applications'"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "@types/react": "19.1.0",
    "@types/react-dom": "19.1.1",
    "eslint": "^9.34.0",
    "typescript": "5.9.2"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
