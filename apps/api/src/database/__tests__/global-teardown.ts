/**
 * Jest Global Teardown for Database Tests
 */

const { globalTeardown } = require('./setup'); // eslint-disable-line @typescript-eslint/no-require-imports, @typescript-eslint/no-unsafe-assignment

module.exports = globalTeardown; // eslint-disable-line @typescript-eslint/no-unsafe-assignment
