import { IncidentStatusChangedEvent } from '../incident-status-changed.event';

describe('IncidentStatusChangedEvent', () => {
  it('should be defined', () => {
    expect(IncidentStatusChangedEvent).toBeDefined();
  });
});
